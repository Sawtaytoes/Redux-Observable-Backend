{
  "name": "@redux-observable-backend/redux-utils",
  "version": "4.3.4",
  "description": "Redux helper functions and middleware.",
  "license": "MITNFA",
  "scripts": {
    "debug": "nodemon --inspect .",
    "lint": "eslint .",
    "start": "yarn && eslint . && nodemon .",
    "test": "ava",
    "test:watch": "ava --watch",
    "test:debug": "ava --verbose",
    "test:debug:watch": "ava --watch --verbose"
  },
  "engines": {
    "node": ">=8.11.4",
    "yarn": ">=1.6.0"
  },
  "_moduleAliases": {
    "$utils": "./utils"
  },
  "ava": {
    "babel": {
      "testOptions": {
        "babelrc": false
      }
    },
    "files": [
      "utils/**/*.test.js"
    ],
    "sources": [
      "**/*.js"
    ]
  },
  "dependencies": {
    "better-module-alias": "^1.0.1"
  },
  "devDependencies": {
    "@redux-observable-backend/core": "^4.3.4",
    "rxjs": "^6.5.2"
  },
  "peerDependencies": {
    "@redux-observable-backend/core": ">4 <5",
    "rxjs": "^6"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Sawtaytoes/Redux-Observable-Backend.git"
  }
}
