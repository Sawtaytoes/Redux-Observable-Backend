{
  "name": "@redux-observable-backend/core",
  "version": "0.4.1",
  "description": "Core packages Redux-Observable Backend.",
  "license": "MITNFA",
  "scripts": {
    "debug": "nodemon --inspect .",
    "lint": "eslint .",
    "start": "yarn && eslint . && nodemon .",
    "test": "ava",
    "test:watch": "ava --watch"
  },
  "engines": {
    "node": ">=8.11.4",
    "yarn": ">=1.6.0"
  },
  "_moduleAliases": {
    "$utils": "./utils"
  },
  "ava": {
    "babel": {
      "testOptions": {
        "babelrc": false
      }
    },
    "files": [
      "utils/**/*.test.js"
    ],
    "sources": [
      "**/*.js"
    ]
  },
  "dependencies": {
    "@redux-observable-backend/eslint-config-base": "^0.0.3",
    "@redux-observable-backend/eslint-config-node": "^0.0.3",
    "@redux-observable-backend/setup-module-aliases": "^0.0.2",
    "ava": "^2.0.0",
    "chalk": "^2.4.2",
    "nodemon": "^1.19.1",
    "rxjs": "^6.5.2"
  }
}
